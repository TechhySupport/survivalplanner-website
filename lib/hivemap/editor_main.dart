import 'package:flutter/material.dart';
import 'main.dart';

// Entry point for the HiveMap Editor when run standalone
void main() {
  runApp(const MaterialApp(title: 'HiveMap Editor', home: HiveMapEditorApp()));
}
